<Window x:Class="ProdLogApp.Views.AddCategory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Agregar Categoría"
        Height="250"
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0864ac">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Designs/Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Label -->
        <Label Content="Nombre Categoría" FontSize="14" Foreground="#FFFFFF"
               HorizontalAlignment="Left" Margin="0,0,0,5" Grid.Row="0"/>

        <!-- TextBox -->
        <TextBox x:Name="NombreCategoriaTextBox" Style="{StaticResource BaseTextBoxStyle}"
                 Height="40" Margin="0,0,0,10" VerticalContentAlignment="Center"
                 HorizontalAlignment="Stretch" Grid.Row="1"/>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Style="{StaticResource ConfirmButtonStyle}" Content="Confirmar"
                    Margin="10" Height="40" Click="Confirmar_Click"/>
            <Button Style="{StaticResource CancelButtonStyle}" Content="Cancelar"
                    Margin="10" Height="40" Click="Cancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
