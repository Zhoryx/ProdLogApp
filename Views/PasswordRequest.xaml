<Window x:Class="ProdLogApp.Views.PasswordRequest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProdLogApp.Views"
        mc:Ignorable="d"
        Title="Seguridad" Height="250" Width="600"
        Background="#0067ac"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Sombra -->
        <DropShadowEffect x:Key="DropShadow"
                          Color="Black"
                          Direction="320"
                          ShadowDepth="5"
                          Opacity="0.2"
                          BlurRadius="10"
        />

        <!-- TextBox Redondeado -->
        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botón Redondeado -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Contenedor central -->
        <Border Background="White"
                CornerRadius="15"
                Padding="30"
                
                Width="500"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Effect="{StaticResource DropShadow}" 
                Height="160">

            <StackPanel  Height="147" Width="448" Margin="-4,-17,-4,-30">

                <TextBlock Text="Esta Acción Requiere Permisos de Administrador"
                           FontSize="14"
                           FontWeight="DemiBold"
                           TextAlignment="Center"
                           Foreground="#333"
                           TextWrapping="Wrap" Height="30" Width="404"
                />

                <TextBox x:Name="txtPassword"
                         Text="Escriba Contraseña Aquí..."
                         Style="{StaticResource RoundedTextBox}"
                         TextAlignment="Center"
                         Width="300"
                         Height="35"
                         TextChanged="txtPassword_TextChanged"
                />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Height="52" >

                    <Button Content="Confirmar"
                            Style="{StaticResource RoundedButton}"
                            Click="Confirmar" Height="33" Width="188"
                            Margin="0,10,10,0"/>
                    <Button Content="Cancelar"
                            Style="{StaticResource RoundedButton}"
                            Click="Cancelar" Height="33" Width="179"
                            Margin="0,10,0,0"
                    />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
